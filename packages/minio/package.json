{
  "name": "@unknown-planet/minio",
  "version": "0.1.0",
  "description": "MinIO S3-compatible blob storage provider for Unknown Planet.",
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/HoshimiAI/unknownplanet.git", "directory": "packages/minio" },
  "publishConfig": { "access": "public" },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc --project tsconfig.build.json", "clean": "tsc --build --clean tsconfig.build.json", "dev": "tsc --project tsconfig.build.json --watch", "lint": "tsc --project tsconfig.json --noEmit", "test": "bun run build && bun test test", "typecheck": "tsc --project tsconfig.json --noEmit" },
  "dependencies": { "@aws-sdk/client-s3": "^3.850.0", "@unknown-planet/core": "workspace:*" }
}
