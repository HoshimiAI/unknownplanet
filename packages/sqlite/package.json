{
  "name": "@unknown-planet/sqlite",
  "version": "0.1.0",
  "description": "SQLite graph and evidence adapters for Unknown Planet.",
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/HoshimiAI/unknownplanet.git", "directory": "packages/sqlite" },
  "publishConfig": { "access": "public" },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc --project tsconfig.build.json", "clean": "tsc --build --clean tsconfig.build.json", "dev": "tsc --project tsconfig.build.json --watch", "lint": "tsc --project tsconfig.json --noEmit", "test": "bun test test", "typecheck": "tsc --project tsconfig.json --noEmit" },
  "dependencies": { "@unknown-planet/core": "workspace:*" },
  "peerDependencies": { "better-sqlite3": ">=11 <13" },
  "peerDependenciesMeta": { "better-sqlite3": { "optional": true } }
}
